import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Mruf Ahmed | MERN & React Native Developer",
  description:
    "Modern portfolio for Mruf Ahmed, a MERN stack and React Native developer building web apps, APIs, dashboards, and mobile experiences."
};

export default function RootLayout({
  children
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
